---
title: Image-map (Resim Üzerine Nokta)
---
## Banner resimleri üzerine nokta


Banner üzerine nokta ile ürün gösterimi yapmak için kullanılır. 
Banner Bloğu gelişmiş ayarlar alanındaki
*<code>DATA1</code>
*<code>DATA2</code>
*<code>DATA3</code>
*<code>DATA4</code>
alanlarına girilen veriye göre resim üzerinde nokta oluşmaktadır. 
<code>25|55|1455</code> gibi <code>soldanyüzde|yukarıdanyüzde|ürünID</code>
şeklinde değer girilmesi gerekmektedir. 


## Kod Alanı
```html title="Smarty"
<div class="col-12">
    {if $SETTING.DISPLAY_TITLE}
        <div class="block-title mb-2">{$BLOCK.TITLE}</div>
    {/if}
    <div class="row">
        {foreach from=$SETTING.IMAGES item=IMAGE}
            {$DATA_AREAS = [
                $IMAGE.DATA1,
                $IMAGE.DATA2,
                $IMAGE.DATA3,
                $IMAGE.DATA4
                ]}
            <div
                class="col-{$IMAGE.COL.ALL} col-sm-{$IMAGE.COL.SM} col-md-{$IMAGE.COL.MD} col-lg-{$IMAGE.COL.LG} col-xl-{$IMAGE.COL.XL} mb-2">
                <div href="{$IMAGE.URL}" class="w-100 d-block position-relative effect-wrapper" aria-label="{$IMAGE.TITLE}">
                    {if $IMAGE.IMG1.PATH || $IMAGE.IMG2.PATH}
                        <picture class="d-flex align-items-center justify-content-center overflow-hidden">
                            {if $IMAGE.IMG1.PATH}
                                <img {if $IS_LAZY_LOAD_ACTIVE == '1'}
                                        src="{$LAZY_LOAD_LOADING_IMAGE}" data-src="{$IMAGE.IMG1.PATH}" class="lazyload w-100" loading="lazy"
                                    {else}
                                        src="{$IMAGE.IMG1.PATH}" class="w-100" 
                                    {/if}
                                       width="{$IMAGE.IMG1.WIDTH}" height="{$IMAGE.IMG1.HEIGHT}" alt="{$IMAGE.TITLE}">
                            {/if}
                        </picture>
                        {foreach from=$DATA_AREAS item=DATA key=key name=name}
                            {assign var="D" value="|"|explode:$DATA}
                            {product id="{$D[2]}" assign="P"}
                            <span class="d-flex align-items-center justify-content-center banner-description position-absolute cursor-pointer border border-circle border-white map-point" style="left:{$D[0]}%; top:{$D[1]}%;">
                                <div class="dropdown">
                                    <span class="dropdown-title border border-circle d-flex align-items-center bg-white" data-toggle="dropdown">
                                    </span>
                                    <div
                                        class="dropdown-menu border-round {if $D[0]>50} right-100 left-auto {/if} {if $D[1]>50} bottom-100 top-auto{/if}">
                                        <div class="w-100 product-item">
                                            <a href="{$P.URL}" class="image-wrapper">
                                                <picture class="image-inner">
                                                    <img src="{$P.IMAGE.SMALL}" class="" loading="lazy">
                                                </picture>
                                            </a>
                                            <div class="d-flex flex-wrap gap-1  map-product-info">
                                                <a href="{$P.URL}" class="w-100 mt-1 fw-semibold text-dark product-title">{$P.TITLE}</a>
                                                <div class="w-100  product-price-wrapper d-flex flex-wrap">
                                                    {if $P.IS_DISPLAY_DISCOUNTED_ACTIVE == 1}
                                                        <div class="mr-1 product-discounted-price text-delete {if $P.DISCOUNT_PERCENT <= 0}d-none{/if}">
                                                            <span class="product-price-not-discounted">{vat price=$P.PRICE_NOT_DISCOUNTED vat=$P.VAT}</span>
                                                            {$P.TARGET_CURRENCY}
                                                        </div>
                                                    {/if}
                                                    <div class="current-price">
                                                        {if $P.DISPLAY_VAT === "1"}
                                                            <strong class="fw-black product-price">{vat price=$P.PRICE_SELL vat=$P.VAT}</strong>
                                                            {$P.TARGET_CURRENCY}
                                                        {else}
                                                            <strong class="fw-black product-price-not-vat">{format price=$P.PRICE_SELL}</strong>
                                                            {$P.TARGET_CURRENCY} <i class="ti-plus"></i> {#vat#}
                                                        {/if}
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </span>
                        {/foreach}
                    {/if}
                </div>
            </div>
        {/foreach}
    </div>
</div>
```
## Scss
```scss title="scss"
@include BLOCK {
    .map-point{
        width: 30px;
        height: 30px;
        .dropdown{
            .dropdown-title{
                width: 20px;
                height: 20px;
            }
             .dropdown-menu{
                max-width: clamp(120px,10vw,170px);
             }
        }
    }
    .left-auto{
        left: auto !important;
    }
    .top-auto{
        top: auto !important;
    }
}
```